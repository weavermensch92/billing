{
  "name": "@gridge/billing-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Gridge Billing Fill Helper — Chrome Extension (MV3) to safely copy VCN card numbers to AI service billing pages",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && node scripts/zip.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "typescript": "^5.9.3",
    "vite": "^5.4.0"
  }
}
